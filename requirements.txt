# Requires Python >= 3.11 (DM_Core.py uses stdlib tomllib).
# tkinter (GUI_Core.py) ships with the standard CPython installer on Windows/macOS;
# on Linux it's a separate OS package (e.g. `apt install python3-tk`).

sentence-transformers==5.1.2
transformers==4.57.1
numpy==2.3.3
textual==8.2.8
pypdf==6.14.2

# Test suite (test_unit.py)
pytest==8.4.1
pytest-asyncio==1.4.0

# test_integration.py only -- parallelizes the live-Ollama suite across worker processes
# (`pytest -n auto tests/test_integration.py`). Each worker pays its own NLP_Core.py model
# load and Ollama request queueing, so the win only shows up once there are enough tests to
# amortize that overhead -- measured ~8% faster than serial on the current 17-test suite,
# not worth it yet on a run of just one or two tests (see docs/testing.md).
pytest-xdist==3.8.0
